{
  "extends": [
    "@commitlint/config-conventional"
  ],
  "rules": {
    "scope-enum": [
      2,
      "always",
      [
        "ml",
        "api",
        "web",
        "admin",
        "infra",
        "docs"
      ]
    ]
  }
}