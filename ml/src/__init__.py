"""
ml.src — Shared production-grade ML library for XAI-Guard.

This package is the single source of truth for all ML classes
used across both the research notebooks and the FastAPI backend.

Sub-packages:
    evaluation  — CDS scoring, McNemar testing, bootstrap CI, analyst utility
    xai         — SHAP & LIME explainer interfaces
    models      — Shared PyTorch model architectures (Transformer, LSTM, etc.)
"""
